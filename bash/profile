# Add personnal bin folder to PATH if exists
if [[ -d "$HOME/bin" ]] ; then
    PATH="$HOME/bin:$PATH"
fi

# Load RVM into a shell session *as a function*
[[ -s "$HOME/.rvm/scripts/rvm" ]] &&
    source "$HOME/.rvm/scripts/rvm"

# Source ~/.bashrc
[[ -f ~/.bashrc ]] &&
    source ~/.bashrc
